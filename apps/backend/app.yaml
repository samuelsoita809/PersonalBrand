runtime: nodejs20

# Environment variables
env_variables:
  NODE_ENV: "production"

# Automatic scaling configuration for Free Tier safety
automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 0
  max_instances: 1

# Handlers for routing
handlers:
  - url: /.*
    script: auto
